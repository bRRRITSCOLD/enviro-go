version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        - os.Unsetenv
        - os.Setenv

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
